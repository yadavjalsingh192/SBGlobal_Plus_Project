# SBGlobal Plus — Vision-Centric Ultra-Deep All-Stages Current-State Audit Master Prompt

**Status:** ACTIVE EXECUTION CONTROL  
**Repository:** `yadavjalsingh192/SBGlobal_Plus_Project`  
**Authorized branch:** `docs/architecture-branch-2`  
**Intended repository path:** `Governing/ULTRA_DEEP_VISION_CENTRIC_ALL_STAGES_CURRENT_STATE_AUDIT_MASTER_PROMPT.md`  
**Purpose:** Freshly audit and correct every completed or in-progress artifact present at Execution Start HEAD, across every project stage completed so far.

---

# 1. Mission

Perform a zero-trust fresh audit of the current remote `docs/architecture-branch-2` branch.

Do not trust previous labels such as `PASS`, `COMPLETE`, `CERTIFIED`, `READY`, `IMPLEMENTED`, `VERIFIED`, `RECERTIFIED`, `9/9`, `41/41`, `181`, or any similar status merely because they are written in files.

The audit must be genuinely:

- word-by-word;
- line-by-line;
- section-by-section;
- table-row-by-table-row;
- structured-key-by-structured-key;
- file-by-file;
- folder-by-folder;
- dependency-by-dependency;
- phase-by-phase;
- implementation-by-implementation;
- test-by-test;
- CI-evidence-by-CI-evidence;
- cross-layer;
- cross-reference;
- sequence-aware;
- Vision-centric.

The objective is not to restore a green status. The objective is to make the repository truth coherent, Vision-aligned, correctly sequenced, traceable, technically valid, testable, and honestly reflected in Registers/State/CI evidence.

---

# 2. Primary Vision — Highest Product Direction

**SBGlobal Plus is an AI-Ready, AI-Extensible, AI-Powered, Enterprise-Grade, Multi-Tenant, Multi-Industry SaaS Platform.**

Canonical product flow:

**One Unified Enterprise Core  
→ Multiple Equal First-Class Industry Suites  
→ Tenants  
→ exactly one Primary Industry + optional Enabled Industries  
→ Branch / Department / Team / Location  
→ Users / Roles / Permissions  
→ Management Systems  
→ Modules / Submodules  
→ Workflows / Transactions  
→ Reports / Analytics  
→ AI-Powered Operations**

All current supported Industries are equal first-class suites.

Healthcare is not the flagship, benchmark, default, architecture template, documentation template, implementation template, or semantic source for sibling industries.

Equal first-class status does **not** require equal table count, module count, workflow count, entity count, or documentation length.

---

# 3. Authority Hierarchy

Use this precedence:

1. Primary Vision.
2. Current explicit invoking-user direction, when compatible with Vision.
3. `Governing/MASTER_INSTRUCTION_v2_5.md`.
4. `Governing/MASTER_PROMPT_v2_5.md`.
5. `RawSourceCorpus/` as immutable knowledge/provenance under its governed internal source precedence.
6. Canonical phase owners after reconciliation:
   - Foundation = WHAT / WHY / WHO.
   - Architecture + ADR = system-level HOW and boundaries.
   - Detailed Design = exact implementation contract.
   - Development = executable realization of approved contracts.
   - Verification / Tests / CI = executable evidence.
7. Registers / State / checkpoints / audit matrices = projections and evidence only.

This audit prompt is an execution-control artifact, not a product-requirement source.

No requirement, business rule, Industry semantic, legal claim, technical truth, count, or PASS status may be justified solely because this prompt says so.

---

# 4. Execution-Contract Freeze

At runtime:

- freshly fetch current remote `docs/architecture-branch-2`;
- record **Execution Start HEAD**;
- record current `main` HEAD;
- fetch this master prompt and record its blob/content SHA;
- freeze that prompt SHA as the execution contract for the current run;
- enumerate the complete repository tree;
- record `RawSourceCorpus/` tree/blob identities;
- inspect open PR state;
- inspect latest relevant CI/workflow state.

If this prompt is edited during remediation, the current run continues under the frozen original prompt SHA.

Do not recursively restart because this file changed.

---

# 5. Current-Scope Discipline

Audit **everything that already exists** and **everything currently claimed completed or in progress**.

Classify missing work into:

### A. `MISSING_WITHIN_CURRENT_CLAIMED_SCOPE`
A required artifact/contract/test/rule/evidence is absent although the current phase/file claims the scope is complete or implemented.

This is a defect.

### B. `INCOMPLETE_WITHIN_CURRENT_IN_PROGRESS_SCOPE`
The current phase is explicitly in progress and the missing item belongs to the active implementation wave.

Record and remediate as needed.

### C. `NOT_YET_STARTED_FUTURE_SCOPE`
The item belongs to a future phase not yet started and is not falsely claimed complete.

Do not create it merely to inflate completion.

### D. `EXTERNAL_CONFIGURATION_INPUT`
The value must legitimately come from customer/provider/legal/contractual/environment input.

Do not invent it.

### E. `APPROVAL_BLOCKED`
The correction is known but requires explicit approval.

Audit and propose; do not enact.

This distinction is mandatory.

---

# 6. Approval Rules

This prompt does not waive any approval requirement.

Do not:

- merge to `main`;
- deploy production;
- force-push or rewrite history;
- change Primary Vision;
- enact an approval-required Management System count exception;
- materially weaken or redefine Tenant/Industry isolation where explicit approval is required;
- materially weaken/change security posture where approval is required;
- resolve approval-blocked `REVIEW_REQUIRED` items without approval.

For approval-blocked items:

**audit fully → identify authority → identify defect → propose exact correction/options → record dependency/blast radius → continue unaffected work → block only dependent claims.**

---

# 7. Repository Safety

Work only on `docs/architecture-branch-2`.

Never:

- modify `RawSourceCorpus/`;
- merge `main`;
- rewrite Git history;
- fabricate commits, tests, workflow runs, PASS results, or PR state;
- commit secrets, tokens, private keys, live credentials, or private production data;
- claim an action occurred when it did not.

Use SHA-safe writes and small logical commits.

If the branch moves concurrently, refetch and reconcile.

---

# 8. Complete Repository Scope Manifest

Classify **every repository file** at Execution Start HEAD as one of:

- `ACTIVE_CANONICAL`
- `ACTIVE_CONTROL`
- `ACTIVE_IMPLEMENTATION`
- `ACTIVE_TEST_OR_VERIFICATION`
- `ACTIVE_CI`
- `ACTIVE_EVIDENCE_OR_STATE`
- `HISTORICAL_OR_SUPERSEDED`
- `RAW_SOURCE_IMMUTABLE`
- `GENERATED_ARTIFACT`
- `NON_TEXT_OR_BINARY`
- `OUT_OF_SCOPE_WITH_EXPLICIT_REASON`

No file may be excluded simply because it is:

- large;
- duplicated;
- generated;
- already certified;
- inconvenient;
- historical-looking;
- status-only.

Distinguish authoritative owner from derived projection.

---

# 9. 100% Per-File Coverage Ledger

For each relevant file record:

- path;
- classification;
- authoritative role;
- blob/content SHA;
- byte size;
- line count for text;
- structured record/key count where useful;
- exact read coverage;
- findings;
- correction disposition;
- correction commit SHA;
- upstream dependencies;
- downstream dependencies;
- downstream revalidation status;
- final file status.

A file may be marked `FULLY_AUDITED` only when 100% of its actual content was read.

If output is truncated, continue by explicit ranges until complete.

Search hits, summaries, matrices, prior audits, file names, and cached context do not count as full coverage.

---

# 10. Word-by-Word / Line-by-Line Text Audit

For Markdown/text inspect:

- title;
- version;
- status;
- dates;
- authority statements;
- every heading/subheading;
- every paragraph;
- every sentence;
- every bullet/list item;
- every table heading;
- every table row/cell;
- every requirement ID;
- every status word;
- every count;
- every path;
- every cross-reference;
- every link/anchor;
- every code/example block;
- every note/warning;
- every amendment/history statement.

Challenge terms including:

`canonical`, `active`, `current`, `complete`, `certified`, `verified`, `implemented`, `ready`, `supported`, `exact`, `required`, `optional`, `future`, `legacy`, `superseded`, `immutable`.

---

# 11. JSON / YAML / Shell Audit

## JSON

Check:

- valid parse;
- stale status;
- stale branch/HEAD;
- inconsistent counts;
- semantic duplicate/conflicting keys;
- dangling references;
- incorrect checkpoint;
- incorrect next action;
- false CI/runtime state.

## YAML / GitHub Actions

Check:

- syntax;
- triggers;
- branch filters;
- path filters;
- permissions;
- service image;
- health checks;
- environment variables;
- secret handling;
- timeout;
- duplicate push/PR execution;
- failure propagation;
- whether claimed behavior is actually reachable.

## Shell

Check:

- strict error handling;
- quoting;
- deterministic ordering;
- dependency checks;
- path safety;
- failure propagation;
- portability assumptions;
- false-success paths;
- secret leakage.

---

# 12. SQL Audit Standard

Audit every SQL migration and SQL verification file.

Inspect every:

- extension;
- schema;
- enum/type;
- function;
- table;
- column;
- data type;
- nullability;
- default;
- primary key;
- foreign key;
- composite Tenant+Industry ownership;
- check constraint;
- unique constraint;
- nullable uniqueness semantics;
- expression/partial index;
- ordinary index;
- partition;
- RLS enable;
- RLS force;
- `USING`;
- `WITH CHECK`;
- role attribute;
- `GRANT`;
- `REVOKE`;
- transaction boundary;
- seed/insert;
- migration dependency;
- identifier truncation risk;
- rerun/idempotency assumption.

For verification SQL detect:

- false positives;
- false negatives;
- ambiguous PL/pgSQL identifiers;
- unstable generated constraint names;
- NULL-semantics bugs;
- wrong table/index/policy reference;
- privileged-role assumptions that hide RLS defects;
- metadata-only tests where behavior proof is required.

---

# 13. Mandatory Audit Sequence

Execute in this order:

0. Repository truth freeze.
1. Governing/control audit.
2. RawSource/source-fidelity audit.
3. Foundation audit.
4. Architecture + ADR audit.
5. Detailed Design audit.
6. Registers/Traceability/State audit.
7. Development implementation audit.
8. Verification/Test/CI audit.
9. Cross-layer reconciliation.
10. Security/isolation adversarial audit.
11. Sequence/dependency audit.
12. Fresh adversarial re-audit.
13. State/checkpoint synchronization.
14. Final remote HEAD/CI/PR verification.
15. Evidence-based current-state gate.

Any substantive upstream correction makes dependent downstream status provisional until revalidated.

---

# 14. Governing Audit

Fully audit all files in `Governing/`.

Verify:

- `MASTER_INSTRUCTION_v2_5.md` and `MASTER_PROMPT_v2_5.md` are coherent;
- Vision is unchanged;
- active vs historical amendments are correctly distinguished;
- authority hierarchy is consistent;
- phase ownership is consistent;
- approval gates are intact;
- RawSource immutability is preserved;
- stale technology does not become active through historical text;
- previous pre-development audit prompts are correctly classified.

Do not casually rewrite governance. Follow its own amendment rules.

---

# 15. RawSource No-Loss Audit

Read the complete `RawSourceCorpus/`.

Classify each meaningful source item:

- `ACTIVE_CANONICAL`
- `VALID_BUT_INCOMPLETELY_PROMOTED`
- `SUPERSEDED_WITH_AUTHORITY`
- `LEGACY_TECHNOLOGY`
- `DUPLICATE_PROVENANCE`
- `REVIEW_REQUIRED`
- `OUTSIDE_CURRENT_CLAIMED_SCOPE`
- `EXTERNAL_CONFIGURATION_INPUT`

Aggressively check for valid stranded requirements involving:

- business capabilities;
- Management Systems;
- workflows;
- rules;
- roles;
- branding;
- website;
- mobile;
- desktop;
- master/reference data;
- localization/country packs;
- demo/seed/media;
- affiliate/referral/commission;
- reports/KPIs;
- security;
- database;
- AI/provider/RAG/agent;
- integrations;
- queue/notification;
- backup/DR;
- observability.

Do not reactivate superseded technology merely because immutable source history retains it.

---

# 16. Foundation Audit

Audit Foundation as WHAT / WHY / WHO.

Verify:

- one Unified Core;
- equal current Industries;
- Tenant model;
- exactly one Primary Industry + optional Enabled Industries;
- canonical MS ownership;
- canonical application surfaces;
- exactly two logical Tenant mobile apps per enabled Industry Experience;
- web and desktop concepts;
- identity/security/commercial intent;
- Core ownership of configuration, metadata, rules, workflow, notification, documents, reporting, integration, AI;
- website/branding/localization/master/demo/media requirements;
- no Healthcare-first bias;
- no missing valid source promotion in claimed complete scope.

---

# 17. Architecture + ADR Audit

Audit Architecture as system-level HOW.

Verify:

- module boundaries;
- ownership boundaries;
- Tenant+Industry Context propagation;
- Core vs Industry separation;
- RBAC primary / ABAC complementary;
- commercial entitlement chain;
- API/event/integration boundaries;
- documents/storage;
- workflow/config/rules/metadata separation;
- AI Gateway;
- offline/sync;
- deployment;
- Data Home/residency;
- backup/DR;
- observability;
- security;
- database architecture;
- current technology authority.

Implementation without a valid owner is `ORPHAN_IMPLEMENTATION` unless backed by a valid labelled completion decision.

---

# 18. Detailed Design Audit

Audit exact implementation contracts:

- entities;
- fields/types/nullability;
- relationships;
- constraints/indexes;
- RLS;
- permissions;
- RBAC/ABAC;
- workflows/transitions;
- validation/errors;
- API/tRPC/REST;
- events/webhooks;
- documents;
- notifications;
- reports/KPI;
- AI/RAG/tools;
- offline/sync;
- configuration;
- entitlement;
- acceptance tests.

Cross-check all Core DD, Industry DD, and deterministic remediation artifacts.

No Industry may borrow sibling semantics.

---

# 19. Current Technology Audit

Derive the active technology baseline from current highest canonical authority.

Expected orientation only:

- Next.js 15;
- React 19;
- TypeScript 5.x;
- Node.js 22+;
- PostgreSQL;
- Payload CMS 3;
- Tailwind + Shadcn;
- React Native + Expo;
- Tauri 2.0;
- tRPC;
- REST/OpenAPI;
- Clerk preferred;
- Auth.js fallback where governed;
- pgvector where supported;
- Vercel + Coolify/Dockerized VPS where appropriate.

Reject stale active references to superseded stacks unless explicitly reintroduced.

---

# 20. Tenant / Industry / Authorization Audit

Verify fail-closed scope classes:

- `PLATFORM_GLOBAL`
- `TENANT_CORE`
- `TENANT_INDUSTRY`
- `EXPLICIT_CROSS_CONTEXT`
- `PUBLIC`

`industryContextId = null` never means all Industries.

Verify effective access chain against current canonical truth:

Authenticate  
→ Tenant  
→ Industry Context  
→ Subscription  
→ License  
→ Session/Device/API Credential  
→ Entitlement Snapshot  
→ RBAC  
→ applicable ABAC/context  
→ Security/Compliance/Residency  
→ Resource/Workflow Rules  
→ Effective Access

RBAC remains primary. ABAC may narrow but never widen a denied permission.

---

# 21. Industry + Management-System Audit

Independently derive the current supported Industry set and canonical MS IDs from active authority.

Do not trust a register count alone.

For every current Industry verify:

- equal first-class status;
- namespace-qualified MS IDs;
- no sibling semantic leakage;
- Industry DD owns Industry semantics;
- implementation owner mappings are correct;
- schema/table prefix mappings are correct;
- cross-MS direct private-table coupling is not introduced;
- cross-Industry direct semantic coupling is not introduced.

Count differences are valid when domain-driven.

---

# 22. Development Implementation Audit

Audit every existing Development artifact.

For each artifact determine:

1. What canonical requirement owns it?
2. Was it created in the correct sequence?
3. Does it implement DD exactly?
4. Does it add undocumented behavior?
5. Does it omit required behavior?
6. Does it create hidden coupling?
7. Is it testable?
8. Is there executable evidence?
9. Is State synchronized?
10. What becomes provisional if it changes?

Do not start unrelated future UI/API phases.

---

# 23. Database Ultra-Deep Audit

Audit every file under `database/` and every database claim elsewhere.

Freshly derive:

- migration inventory;
- migration ordering;
- verification inventory;
- schemas;
- table counts;
- canonical MS-owner counts;
- RLS registry rows;
- roles;
- grants;
- indexes;
- constraints;
- extensions;
- CI evidence.

Do not trust previously written `9/9`, `41/41`, `181`, or migration-range claims without recounting.

Verify:

- Tenant+Industry ownership;
- forced RLS;
- correct scope helpers;
- parent policies;
- NOBYPASSRLS runtime roles;
- migration/admin segregation;
- physical storage boundary;
- Integration credential boundary;
- AI Gateway DB boundary;
- worker/service roles;
- append-only evidence;
- partition/global identity design;
- cross-MS service boundary;
- no cross-Industry leakage;
- no broad GRANT contradicting intended verification;
- nullable uniqueness correctness;
- same-context composite FK correctness.

---

# 24. Workflow / Automation / Notification Audit

## Workflow

Verify:

- versioned definitions;
- instances;
- tasks/approvals;
- transition evidence;
- configurable state names;
- expected-version concurrency;
- authorization revalidation;
- audit.

## Automation

Verify:

- versioned definitions;
- event/schedule/manual triggers;
- safe rule evaluation;
- only governed OperationContract/Workflow side effects;
- idempotency;
- run evidence;
- no arbitrary-code escape.

## Notification

Verify:

- templates;
- channels;
- locale;
- safe preview;
- routing;
- TenantIntegration boundary;
- delivery status;
- attempt evidence;
- provider secrets not stored in notification tables.

---

# 25. Integration / API / Event / Webhook Audit

Verify:

- OperationContract;
- tRPC first-party boundary;
- REST/OpenAPI external boundary;
- schema versions;
- idempotency;
- rate classes;
- API credentials;
- IntegrationDefinition;
- TenantIntegration;
- CredentialReference;
- IntegrationCapability;
- ProviderAdapter;
- SyncCursor;
- timeout/retry/circuit behavior;
- normalized errors;
- webhook authenticity;
- delivery evidence;
- event envelope context;
- outbox behavior;
- adapter prohibition on direct domain-table writes;
- data-transfer/residency/vendor-risk behavior.

Do not invent provider guarantees.

---

# 26. Document / Storage Audit

Verify:

- DocumentMeta is the authorization/business metadata boundary;
- StorageObject is physical storage metadata only;
- storage path never authorizes;
- private storage access is service-bound;
- ACL/context behavior;
- upload session;
- signed download;
- malware/content checks where governed;
- retention/legal hold/erasure;
- Data Home/residency;
- wrong-context denial;
- runtime DB role separation.

---

# 27. AI / RAG / Agent Audit

Verify:

- provider/model catalog;
- AI capabilities;
- Tenant AI config;
- Industry AI config;
- AI policies;
- prompts;
- provisioning snapshots;
- assistants;
- RAG source/chunks;
- embeddings;
- conversations/messages;
- memory;
- usage/cost;
- tool definitions;
- agents/runs/steps;
- human approvals;
- media requests;
- sensitivity;
- residency;
- retention;
- budget/entitlement;
- RAG ACL;
- prompt-injection resistance;
- AI tool permission <= acting principal;
- no direct general-app access where AI Gateway boundary forbids it.

---

# 28. Security / Privacy / Offline Audit

Audit:

- Zero-Trust posture;
- RBAC/ABAC;
- session/device;
- MFA/step-up where governed;
- rate limits;
- RLS;
- encryption/secret handling;
- API/webhook authenticity;
- support elevation;
- audit logs;
- privacy-safe logs;
- retention/legal hold/erasure;
- residency;
- backup/restore;
- vendor risk;
- AI security;
- export controls.

Offline:

- operation classification;
- cached scope;
- original device/time/context;
- replay reauthorization;
- stale entitlement;
- conflict policy;
- no unsafe LWW;
- wrong-context queue denial;
- device revocation.

---

# 29. Branding / Website / Theme / Defaults Audit

Check source-to-canonical promotion and current consistency for:

- platform brand;
- canonical tagline;
- logo/media provenance;
- colors;
- typography;
- spacing/radius/shadow;
- theme;
- accessibility;
- breakpoints;
- public SaaS website;
- Tenant branding/white-label;
- Industry presentation;
- report/PDF/print;
- app icon/favicon/domain/contact branding;
- preview/publish/version/rollback;
- localization;
- country packs;
- India defaults where governed;
- currency/timezone/date/number/address/phone abstractions;
- master/reference data;
- demo/seed;
- synthetic/no-PII content;
- media licensing/provenance/alt text/file safety.

---

# 30. Traceability / Stable-ID / Cross-Reference Audit

Validate the current-stage chain:

Source/User  
→ Foundation  
→ Architecture/ADR  
→ Detailed Design  
→ Development where started  
→ Verification/Test  
→ CI/runtime where claimed  
→ State/Checkpoint

Detect:

- orphan requirement;
- orphan implementation;
- orphan test;
- false traceability;
- duplicated authority;
- stale reference;
- dangling ID;
- broken link/anchor;
- status derived only from a matrix/checklist.

Preserve stable IDs.

If an ID must change:

- create a new ID;
- mark old ID historical/superseded;
- create old→new mapping;
- update dependents;
- revalidate no dangling references.

---

# 31. Sequence / Blast-Radius Audit

Canonical dependency sequence:

Vision  
→ Governance/Source reconciliation  
→ Foundation  
→ Architecture/ADR  
→ Detailed Design  
→ Development  
→ Verification/Tests  
→ CI/runtime evidence  
→ State/Checkpoint  
→ next implementation wave

For every material correction record:

- authority;
- defect;
- upstream affected files;
- downstream affected files;
- IDs;
- Tenant/Industry/security/commercial impact;
- tests to rerun;
- certifications/statuses invalidated;
- correction commit.

Never repair a substantive defect only by changing status/count/checkpoint text.

Fix underlying truth first.

---

# 32. Placeholder / REVIEW_REQUIRED Sweep

Search active files for:

`REVIEW_REQUIRED`, `TBD`, `TBC`, `TODO`, `FIXME`, `OPEN`, `placeholder`, `later`, `future decision`, `as appropriate`, `where appropriate`, `where justified`, `if needed`, `developer decides`, `implementation decides`, `module policy`.

Classify each:

- `RESOLVED`
- `LEGITIMATE_HISTORY`
- `EXTERNAL_CONFIGURATION_INPUT`
- `NOT_YET_STARTED_FUTURE_SCOPE`
- `REAL_FOUNDATION_GAP`
- `REAL_ARCHITECTURE_GAP`
- `REAL_DD_GAP`
- `REAL_IMPLEMENTATION_GAP`
- `APPROVAL_BLOCKED`

No current-stage PASS if a material real current-scope gap remains.

---

# 33. State / Manifest / Checkpoint Audit

Validate:

- `State/PROJECT_STATE.md`;
- `State/PROJECT_MANIFEST.json`;
- handoff files;
- `Registers/D-CHECKPOINT.md`;
- `Registers/D-INDEX.md`;
- `Registers/D-CHANGELOG.md`;
- `BACKUP_METADATA.json`;
- Development checkpoints/matrices.

Check:

- parse;
- branch;
- HEAD;
- phase;
- checkpoint;
- counts;
- CI state;
- runtime evidence;
- PR state;
- next action.

Human-readable PASS cannot override stale machine-readable state.

---

# 34. CI / Runtime Evidence Audit

Inspect latest relevant GitHub Actions runs.

For each relevant run record:

- run ID;
- exact `head_sha`;
- event type;
- status;
- conclusion;
- job;
- failing step;
- relevant logs;
- first deterministic root cause.

If failed:

**classify migration defect vs verification defect vs CI defect → fix root cause → commit → inspect new run → repeat while tools permit.**

Do not claim PASS because:

- workflow exists;
- migration files exist;
- an older commit passed;
- migrations applied before later verification failed;
- a run is queued/in-progress;
- static review appears clean.

Final runtime PASS must match the executable implementation HEAD being claimed.

---

# 35. Database CI Failure Loop

For the current Database phase:

1. Fetch latest branch.
2. Fetch newest relevant `Database Verify` run.
3. If failed, fetch job/steps/logs.
4. Isolate first deterministic failure.
5. Classify it.
6. Fix the smallest complete root cause.
7. Commit.
8. Fetch the new run.
9. Repeat until PASS or a genuine blocker.
10. Confirm **all migrations and all verification SQL** actually ran.
11. Confirm no later executable commit invalidated the PASS.

Only then may Database runtime verification be marked PASS.

---

# 36. Adversarial Isolation Attacks

Actively attempt to disprove:

- Tenant A → Tenant B isolation;
- same-Tenant sibling Industry isolation;
- missing Industry Context safety;
- disabled Industry safety;
- wrong-context document access;
- wrong-context event/webhook;
- offline wrong-context replay;
- stale entitlement;
- revoked credential;
- support elevation abuse;
- cross-context export/report;
- worker missing context;
- pooled DB connection stale context;
- AI cross-tenant retrieval;
- AI cross-industry retrieval;
- AI tool escalation;
- prompt injection attempting scope/tool widening;
- explicit cross-context action lacking dedicated permission/audit.

---

# 37. Correction Authority

You may correct any non-RawSource file when evidence requires it, including:

- Governing under its own amendment rules;
- Foundation;
- Architecture/ADR;
- Detailed Design;
- Registers;
- State;
- Development docs;
- SQL migrations;
- verification SQL;
- shell scripts;
- CI workflows;
- other existing implementation artifacts.

Rules:

- preserve valid content;
- smallest complete correction;
- no rewrite merely for style;
- correct authoritative upstream owner before or together with downstream implementation;
- label legitimate Development completion decisions;
- update tests and traceability;
- revalidate downstream;
- never invent laws, statutory rules, provider guarantees, contracts, or certifications.

---

# 38. Status Invalidation Propagation

Substantive corrections propagate:

- Governing → dependent Foundation/Architecture/DD/Development/Tests/State provisional.
- Foundation → dependent Architecture/DD/Development/Tests/State provisional.
- Architecture/ADR → dependent DD/Development/Tests/State provisional.
- DD → dependent Development/Tests/State provisional.
- Development → Verification/CI/State provisional.
- Test/CI correction → dependent status provisional.

Revalidate before restoring PASS.

---

# 39. Final Adversarial Re-Audit

After corrections, assume the repository is still wrong.

Try to find at least:

- one stranded valid source requirement;
- one stale active technology reference;
- one Foundation/Architecture/DD contradiction;
- one orphan implementation;
- one orphan test;
- one false PASS;
- one stale count;
- one broken cross-reference;
- one cross-Industry leak;
- one direct sibling-MS coupling;
- one RLS bypass;
- one overly broad GRANT;
- one nullable uniqueness bug;
- one migration dependency bug;
- one verification false positive/negative;
- one CI HEAD mismatch;
- one stale manifest;
- one AI permission escalation;
- one secret exposure;
- one document authorization leak;
- one unresolved approval blocker.

Any surviving P0/P1 blocks PASS.

---

# 40. Severity

## P0

- Tenant/Industry isolation breach;
- privilege bypass;
- secret exposure;
- destructive data-loss design;
- RawSource mutation;
- unauthorized main merge/deployment;
- fabricated evidence.

## P1

- Vision contradiction;
- incorrect canonical ownership;
- missing required current-scope contract;
- implementation inconsistent with DD;
- broken current migration/runtime verification;
- false certification;
- material traceability loss;
- broken commercial/security/AI/document boundary.

## P2

Significant but not necessarily blocking unrelated current scope.

## P3

Minor wording/formatting/non-semantic issue.

---

# 41. Required Evidence Artifacts

Produce repository evidence covering at minimum:

1. Execution Start HEAD.
2. Frozen audit-prompt SHA.
3. main HEAD.
4. repository scope manifest.
5. per-file 100% coverage ledger.
6. RawSource integrity result.
7. source-promotion/no-loss result.
8. Governing result.
9. Foundation result.
10. Architecture/ADR result.
11. Detailed Design result.
12. Development implementation result.
13. database migration result.
14. RLS/roles/GRANT result.
15. independently derived Industry/MS/table matrix.
16. Workflow/Notification/Integration/Document/AI result.
17. traceability result.
18. stable-ID/cross-reference result.
19. placeholder/REVIEW_REQUIRED result.
20. isolation attack matrix.
21. verification/test result.
22. CI/runtime result with run IDs + exact HEADs.
23. defect register.
24. correction register with commit SHAs.
25. blast-radius register.
26. invalidation/revalidation record.
27. machine-readable state validation.
28. final adversarial audit.
29. Final Substantive Audited HEAD.
30. Final CI-Verified HEAD where applicable.
31. Final Metadata/Closure HEAD if different.
32. main unchanged verification.
33. PR truth.
34. final current-state gate.
35. exact next governed action.

---

# 42. Final Current-State Gate

PASS only when:

- every relevant active file has proven full coverage;
- all completed-stage claims are evidence-supported;
- all in-progress-stage statuses are truthful;
- no current-scope P0/P1 remains;
- Vision is consistent;
- phase ownership is consistent;
- valid source knowledge is not lost inside already-claimed complete scope;
- stale technology does not contaminate active truth;
- Tenant+Industry isolation is fail-closed;
- Industry/MS ownership is consistent;
- implementation is traceable to DD/decisions;
- verification tests are logically valid;
- runtime PASS claims match exact executable HEAD;
- State/Manifest/Register truth matches repository/CI truth;
- RawSource remains unchanged;
- main remains unmerged unless separately approved.

PASS wording:

`VISION-CENTRIC ALL-STAGES CURRENT-STATE AUDIT — PASS`  
`CLAIMED COMPLETED STAGES — SUPPORTED`  
`CURRENT IN-PROGRESS STAGE — CONSISTENT AT VERIFIED CHECKPOINT`  
`NO CURRENT-SCOPE P0/P1 — SUPPORTED`  
`NEXT GOVERNED ACTION — <exact action>`

Blocked wording:

`VISION-CENTRIC ALL-STAGES CURRENT-STATE AUDIT — BLOCKED`  
`UNSUPPORTED CURRENT CLAIMS — <scope>`  
`P0/P1 BLOCKERS — <exact blockers>`  
`NEXT CORRECTIVE ACTION — <exact action>`

Do not turn honest `NOT_YET_STARTED_FUTURE_SCOPE` into a blocker.

---

# 43. Final Response

Return concise but evidence-based final output containing:

- Execution Start HEAD;
- Final Substantive Audited HEAD;
- Final CI-Verified HEAD if available;
- Final Metadata/Closure HEAD if different;
- total files classified;
- fully audited active files;
- defects by severity;
- corrected defects;
- unresolved blockers;
- independently derived Industry/MS/database result;
- current implementation/runtime result;
- RawSource integrity;
- main/PR truth;
- final gate;
- exact next action.

Never say `line-by-line audited` without coverage proof.

---

# 44. Execution Command

Execute this master prompt completely against the freshly fetched current remote `docs/architecture-branch-2` branch.

Start from zero trust.

Read every required file completely.

Audit every completed and in-progress stage.

Correct every permitted current-scope defect.

Do not start unrelated future phases.

Revalidate every affected downstream layer.

Use real Git/CI evidence.

Do not merge `main`.

Do not modify `RawSourceCorpus`.

Stop only at the evidence-based current-state gate.
